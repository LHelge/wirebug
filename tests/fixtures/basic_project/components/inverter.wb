use Hv2p from "connectors.wb";

component Inverter {
    connector hv: Hv2p {
        pub port dc_pos "DC+" pin 1;
        pub port dc_neg "DC-" pin 2;
    }
}

view pinout "Inverter Pinout" {
    grid: 20;

    include hv at (0, 0);
}
