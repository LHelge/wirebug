use Hv2p from "connectors.wb";

component Inverter {
    pub port dc_pos "DC+";
    pub port dc_neg "DC-";

    connector hv: Hv2p {
        pin 1: dc_pos;
        pin 2: dc_neg;
    }
}

view pinout "Inverter Pinout" {
    grid: 20;

    include hv at (0, 0);
}
